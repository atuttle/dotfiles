In all interactions sacrifice grammar for the sake of concision.

At the end of each plan, give me a list of unresolved questions to answer, if any.

For nodejs projects prefer pnpm over npm, but use package-lock.json/pnpm-lock.yaml file existance to detect existing package manager usage and use the same one.
